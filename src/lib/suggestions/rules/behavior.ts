import type { Rule } from '../types'

// Rule 19 (aggressive-behavior-recurring) requires multi-inspection history.
// Not implemented in v1. SuggestionContext.recentInspections is available for when this is added.
// export const aggressiveBehaviorRecurring: Rule = (_ctx) => null
