# Supabase locale — valori da `npx supabase status` dopo aver fatto `supabase start`
# Copia questo file in .env.local e sostituisci i valori reali.
# .env.local NON va committato nel repo.

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-local-anon-key-here
